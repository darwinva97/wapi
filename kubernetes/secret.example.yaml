apiVersion: v1
kind: Secret
metadata:
  name: wapi-secrets
  namespace: wapi
type: Opaque
stringData:
  # IMPORTANT: This is an EXAMPLE file. Replace these values with your actual credentials.
  # For production, use proper secret management:
  # - Sealed Secrets: https://github.com/bitnami-labs/sealed-secrets
  # - External Secrets Operator: https://external-secrets.io/
  # - Cloud provider secret managers (AWS Secrets Manager, GCP Secret Manager, etc.)
  
  DATABASE_URL: "libsql://your-database.turso.io"
  DATABASE_AUTH_TOKEN: "your-turso-auth-token-here"
  BETTER_AUTH_SECRET: "your-secret-key-must-be-minimum-32-characters-long"

# SECURITY NOTES:
# 1. DO NOT commit this file with real credentials to version control
# 2. Copy this file to k8s/secret.yaml and fill with real values
# 3. k8s/secret.yaml is in .gitignore to prevent accidental commits
# 4. For CI/CD, inject secrets from your pipeline's secret store
# 5. Consider using external secret management solutions for production
