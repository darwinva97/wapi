apiVersion: v1
kind: ConfigMap
metadata:
  name: wapi-config
  namespace: wapi
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  PORT: "3000"
  HOSTNAME: "0.0.0.0"
  # Update this with your actual domain
  BETTER_AUTH_URL: "https://your-domain.com"
